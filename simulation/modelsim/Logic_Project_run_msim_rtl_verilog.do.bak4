transcript on
if {[file exists rtl_work]} {
	vdel -lib rtl_work -all
}
vlib rtl_work
vmap work rtl_work

vlog -vlog01compat -work work +incdir+G:/Sem\ 7/Logic\ 2/Project {G:/Sem 7/Logic 2/Project/adder.v}

